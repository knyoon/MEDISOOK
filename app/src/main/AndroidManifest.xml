<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.medisook.app">

    <application
        android:windowSoftInputMode="adjustPan"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Medisook">

        <activity
            android:name=".SearchActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DruginfoActivity"
            android:exported="true">
        </activity>
<!--        <activity android:name=".RedpopActivity"-->
<!--            android:theme="@android:style/Theme.Dialog" />-->
<!--        이렇게 설정해놓으면 팝업이 현재 화면 위에 올라감.-->
        <activity
            android:name=".RecordpopActivity"
            android:theme="@android:style/Theme.Translucent">
        </activity>


        <activity
            android:name=".TodatePickerActivity"
            android:theme="@style/Theme.AppCompat">
        </activity>

        <activity
            android:name=".FromdatePickerActivity"
            android:theme="@style/Theme.AppCompat">
        </activity>
    </application>
    </manifest>